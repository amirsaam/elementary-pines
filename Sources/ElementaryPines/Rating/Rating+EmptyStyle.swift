/// The style for unfilled (empty) rating icons.
public enum PinesRatingEmptyStyle: Sendable {
    /// Stroke only, no fill — the default Pines outlined look.
    case outlined
    /// Filled with a muted gray — the "greyed out" Pines variant.
    case filled
}
