/// Namespace for Pines UI components. Every component is a static function
/// on this enum (e.g. `Pines.button("Click me")`, `Pines.modal(title: "Hi") { … }`).
///
/// Pines components require Alpine.js and Tailwind CSS to be loaded in your
/// `HTMLDocument.head`. See README → Installation.
public enum Pines {}
